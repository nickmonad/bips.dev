- [x] do some styling
- [x] measure search index
    * 8MB
- [*] parse <pre> header and extract info to page.* attributes
    - [ ] read list of files from stdin
- [ ] fix mediawiki to markdown conversions
    * analyze parsing results (by type of element / errors) - can we half-ass a few cases?
    * tables seem to be the most broken at the moment
    * code blocks seem to render as "list" of single lines, not a full block
